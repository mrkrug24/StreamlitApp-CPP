# 🚗 Прогнозирование стоимости автомобилей

🌐 **[Приложение доступно по ссылке](https://predictcarprice24.streamlit.app/)**

Краткое описание: Исследовательский анализ данных, построение модели прогнозирования стоимости автомобилей и развёртывание интерактивного приложения на Streamlit.

---

## 📊 Что было сделано

1. **Базовый EDA** – анализ данных, обработка пропусков и дубликатов, автоматизированный отчёт с помощью `ydata-profiling`.
2. **Предобработка признаков** – извлечение числовых значений из текстовых полей (`mileage`, `engine`, `max_power`), кодирование категориальных признаков.
3. **Визуализация** – построение распределений, корреляционных матриц и анализ взаимосвязей.
4. **Моделирование** – обучение и сравнение нескольких регрессионных моделей, подбор гиперпараметров.
5. **Разработка ML-пайплайна** – создание воспроизводимого конвейера предобработки и предсказания.
6. **Streamlit-приложение** – интуитивный веб-интерфейс для анализа данных и получения прогнозов.

---

## 📈 Результаты

**Лучшая модель:** Ridge Regression (`alpha=10`)  
**Метрики на обучающих данных:**  
- R² = 0.87  
- MSE = 2e+11  

**Интерпретация:**  
- Наибольшее влияние на цену оказывают: `year`, `max_power`, `engine`.  
- Положительный вклад: дизельное топливо (`fuel_Diesel`), автоматическая коробка (`transmission_Automatic`).  
- Отрицательное влияние: пробег (`km_driven`).

---

## 🚀 Что дало наибольший прирост качества

1. **Логарифмирование целевой переменной** – уменьшило асимметрию распределения цены.  
2. **One-Hot Encoding категориальных признаков** – позволило учесть влияние типа топлива, коробки передач и др.  
3. **Регуляризация (Ridge)** – улучшила обобщающую способность модели.

---

## ⚠️ Что не удалось и почему

1. **Полная обработка признака `torque`** – из-за разнообразия форматов данных (`"200Nm@ 1750-3000rpm"`, `"22.4 kgm at..."`) требуется отдельный парсер, что выходит за рамки проекта.  
2. **Детальный разбор поля `name`** – содержит информацию о марке, модели и комплектации; выделение отдельных признаков требует дополнительного анализа.

---

## 🎯 Оценка разработанного сервиса

### ✅ Удобство использования
- Чёткая навигация по разделам, адаптивный интерфейс.  
- Поддержка одиночного и пакетного (CSV) ввода.  
- Быстрые и понятные прогнозы.

### 📊 Визуализация
**Хорошо получилось:**  
- Интерактивные графики (Plotly) с детализацией по наведению.  
- Treemap важности признаков, тепловая карта корреляций.  
- Сравнительные гистограммы и боксплоты.

**Менее удачно:**  
- Ограниченная кастомизация графиков.  
- Нет сравнения нескольких моделей в одном интерфейсе.

### 🔍 Ограничения и проблемы
1. **Производительность** – может снижаться при загрузке больших CSV.  
2. **Валидация ввода** – не все поля проверяются в реальном времени.  
3. **Одна модель** – только Ridge Regression, без выбора альтернатив.


---

## 📬 Заключение

Сервис представляет собой работоспособное MVP для прогнозирования цен на автомобили, сочетающее аналитические возможности и удобный интерфейс.

*Для начала работы запустите:*  
```bash
streamlit run app.py
```
